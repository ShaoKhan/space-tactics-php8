{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/buildings.css') }}" rel="stylesheet"/>
{% endblock %}
{% block title %}Space-Tactics Main{% endblock %}
{% block body %}

    {% include 'parts/ingame_mainmenu.html.twig' %}
    <section id="content" class="main buildings-container">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-2 col-md-12">
                    {% if app.user is not null %}
                        {% include 'parts/sidebar.html.twig' %}
                    {% endif %}
                </div>
                <div class="col-lg-10 col-md-12">
                    <section>
                        <div class="row">
                            <div class="col-lg-7 col-md-12">
                                <div class="row">
                                    <div class="col-lg-6 mt-1">
                                        <h3>{{ user.username }}, anderen voraus sein ist wichtig! </h3>
                                    </div>
                                    <div class="col-lg-6">
                                        <select name="planet" class="form-control planet-switcher-select">
                                            {% for planet in planets %}

                                                <option
                                                        value="{{ planet.slug }}"
                                                        style="color: #000;"
                                                        {{ planet.slug == slug ? 'selected' : '' }}
                                                >
                                                    {{ planet.name }}
                                                    [{{ planet.system_x }}:{{ planet.system_y }}:{{ planet.system_z }}]
                                                </option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="container-fluid mt-3">
                            <div class="buildings">

                                {% for sc in science %}
                                    {{ dump(sc) }}
                                   {# <div class="building">
                                        <div class="building-headline">
                                            <span>{{ building.name|trans({}, 'buildings') }}</span> |
                                            <a href="#" title="{{ 'recycle'|trans({},'buildings') }}">x</a>
                                        </div>
                                        <div class="building-content">
                                            <div class="col-6 building-content-image">
                                                <img src="/{{ building.image }}" alt="Planet image"/>
                                            </div>
                                            <div class="col-6 building-content-specs">
                                                {% set nextLevel = building.building_level + 1 %}
                                                <div>{{ 'production'|trans({},'ui') }}: {{ building.production }}
                                                    / {{ 'hour'|trans({},'ui') }}</div>
                                                <h4> {{ 'costs'|trans({},'ui') }} {{ 'stage'|trans({},'ui') }} {{ nextLevel }}
                                                    :</h4>
                                                <div class="energy">{{ 'energy'|trans({},'resources') }}
                                                    : {{ building.nextEnergyCosts }}</div>
                                                <div class="metal">{{ 'metal'|trans({},'resources') }}
                                                    : {{ building.BuildCosts.metal }}</div>
                                                <div class="crystal">{{ 'crystal'|trans({},'resources') }}
                                                    : {{ building.BuildCosts.crystal }}</div>
                                                <div class="deuterium">{{ 'deuterium'|trans({},'resources') }}
                                                    : {{ building.BuildCosts.deuterium }}</div>
                                            </div>
                                        </div>
                                        <div class="col-12 building-button mt-1">
                                            <button type="button"
                                                    class="btn btn-light">
                                                {{ 'upgrade_to'|trans({},'buildings') }} {{ nextLevel }}
                                            </button>
                                        </div>
                                    </div>#}
                                {% endfor %}
                            </div>
                        </div>
                    </section>
                </div>
            </div>
        </div>
    </section>
    {% include 'parts/footer.html.twig' %}
{% endblock %}
